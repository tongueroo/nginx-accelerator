#!/bin/bash

./build
luarocks make
export PATH=$(pwd)/ngx_openresty/nginx/sbin:$PATH
nginx -p `pwd`/nginx/ -c nginx.conf -s reload || nginx -p `pwd`/nginx/ -c nginx.conf
sleep 2
curl -v http://localhost:8080/test
tail -f nginx/logs/error.log
